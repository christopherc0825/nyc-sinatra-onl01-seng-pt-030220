<form method="POST" action="/figures/<%= @figure.id %>">
    <input type="hidden" id="hidden" name="_method" value="patch">

    <p>
        <label for="figure_name">Name</label>
        <input type="text" id="figure_name" name="figure[name]" value="<%= @figure.name %>">
    </p>
    <br><br>

    <p>
        <label><h4>Edit Landmarks:</h4><label>
        <br>
        <% Landmark.all.each do |landmark| %>
            <label for="<% landmark.name %>"><%= landmark.name %></label>
            <input type="checkbox" name="figure[landmark_ids][]" value="<%= landmark.id %>" id="<%= landmark.name %>" <%= 'checked' if @figure.landmarks.include?(landmark) %>>
        <% end %>
    </p>

    <p>
        <label for="new_landmark">Add New:</label>
        <input type="text" id="new_landmark" name="landmark[name]">
    </p>
    <br><br>

    <p>
        <label><h4>Edit Titles</h4></label>
        <% Title.all.each do |title| %>
            <label for="<%= title.name %>"><%= title.name %></label>
            <input type="checkbox" name="figure[title_ids][]" value="<%= title.id %> id="<%= title.name %> <%= 'checked' if @figure.titles.include?(title) %>>
        <% end %>
    </p>
    <br>

    <p>
        <label for="new_title">Add New Title:</label>
        <input type="text" id="new_title" name="title[name]">
    </p>
    <br><br>

    <p>
        <input type="submit" value="Edit Figure">
    </p>
</form>